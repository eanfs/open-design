import type { WebPresentation } from '../presentation';

export const auroraWebPresentation: WebPresentation = {
  product: 'aurora',
  productName: 'Aurora',
  capabilities: {
    showRuntimeControls: false,
    showByokControls: false,
    showDesktopEntrypoints: false,
  },
};
